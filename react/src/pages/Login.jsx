import { useState } from 'react'
import './Login.css'

const LOGO_URL='https://res.cloudinary.com/dwmjz9csc/image/upload/v1787423047/WhatsApp_Image_2026-08-21_at_19.39.36-removebg-preview_qelqnz.png'
export default function Login({onSuccess}){
 const [signup,setSignup]=useState(false),[step,setStep]=useState('phone'),[name,setName]=useState(''),[phone,setPhone]=useState(''),[email,setEmail]=useState(''),[otp,setOtp]=useState(''),[error,setError]=useState(''),[message,setMessage]=useState('')
 const reset=next=>{setSignup(next);setStep('phone');setError('');setMessage('');setOtp('')}
 const sendOtp=e=>{e.preventDefault();setError('');if(signup&&!name.trim())return setError('Please enter your full name.');if(!/^\d{10}$/.test(phone))return setError('Please enter a valid 10-digit phone number.');setMessage(`We sent a 6-digit verification code to +1 ${phone}`);setStep('otp')}
 const verify=e=>{e.preventDefault();setError('');if(!/^\d{6}$/.test(otp))return setError('Please enter the 6-digit OTP.');onSuccess?.()}
 return <main className="auth-page"><div className="auth-visual"><div className="visual-brand"><img src={LOGO_URL} alt="Clean America Dallas"/><span>Clean America<br/><b>Dallas</b></span></div><div className="visual-copy"><span>OUTDOOR SERVICES, SIMPLIFIED</span><h2>A cleaner space<br/><em>starts here.</em></h2><p>Book trusted local service professionals in a few simple steps.</p></div><div className="visual-chip"><b>✓</b><span><strong>Trusted service</strong><small>Professional & reliable</small></span></div></div><section className="auth-card"><div className="mobile-brand"><img src={LOGO_URL} alt="Clean America Dallas"/><span>Clean America <b>Dallas</b></span></div><div className="auth-heading"><span className="kicker">{signup?'CREATE ACCOUNT':'WELCOME BACK'}</span><h1>{signup?'Create your account':'Sign in'}</h1><p>{signup?'Enter your details and verify your phone to get started.':'Sign in securely using a one-time password sent to your phone.'}</p></div>{step==='otp'?<form className="auth-form" onSubmit={verify}><button type="button" className="back-link" onClick={()=>{setStep('phone');setError('')}}>← Change phone number</button><div className="otp-box"><span>VERIFY YOUR PHONE</span><strong>{message}</strong></div><label>Verification code<input className="otp-input" value={otp} onChange={e=>setOtp(e.target.value.replace(/\D/g,'').slice(0,6))} inputMode="numeric" autoComplete="one-time-code" placeholder="000000" maxLength="6" autoFocus/><small>Enter the 6-digit code sent to your phone.</small></label><button className="primary" type="submit">Verify & Continue <span>→</span></button><button type="button" className="text-button" onClick={()=>setMessage(`A new code was sent to +1 ${phone}`)}>Resend OTP</button></form>:<form className="auth-form" onSubmit={sendOtp}>{signup&&<label>Full name <span className="required">Required</span><input value={name} onChange={e=>setName(e.target.value)} placeholder="Enter your full name" autoComplete="name"/></label>}<label>Phone number <span className="required">Required</span><div className="phone-field"><span>+1</span><input value={phone} onChange={e=>setPhone(e.target.value.replace(/\D/g,'').slice(0,10))} placeholder="214 555 0188" inputMode="numeric" autoComplete="tel"/></div></label>{signup&&<label>Email address <span className="optional">Optional</span><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" autoComplete="email"/></label>}<button className="primary" type="submit">Send OTP <span>→</span></button></form>}{error&&<div className="error">{error}</div>}{message&&step==='phone'&&<div className="notice">{message}</div>}<div className="auth-divider"><span>or</span></div><button className="google" type="button" onClick={()=>setMessage('Google sign-in will be connected to the production OAuth provider.') }><span>G</span> Continue with Google</button><p className="account-switch">{signup?'Already have an account?':'New to Clean America Dallas?'} <button type="button" onClick={()=>reset(!signup)}>{signup?'Sign in':'Create an account'}</button></p><p className="legal">By continuing, you agree to our <u>Terms</u> and <u>Privacy Policy</u>.</p></section></main>
}
