import { useState } from 'react'
import './Login.css'

const LOGO_URL='https://res.cloudinary.com/dwmjz9csc/image/upload/v1787423047/WhatsApp_Image_2026-08-21_at_19.39.36-removebg-preview_qelqnz.png'
export default function Login({onSuccess}){
 const [mode,setMode]=useState('signin'),[method,setMethod]=useState('phone'),[step,setStep]=useState('form')
 const [name,setName]=useState(''),[phone,setPhone]=useState(''),[email,setEmail]=useState(''),[otp,setOtp]=useState(''),[error,setError]=useState(''),[message,setMessage]=useState('')
 const reset=(nextMode=mode)=>{setMode(nextMode);setStep('form');setError('');setMessage('');setOtp('')}
 const sendOtp=e=>{e.preventDefault();setError('');if(!/^\d{10}$/.test(phone))return setError('Please enter a valid 10-digit phone number.');if(mode==='signup'&&!name.trim())return setError('Please enter your full name.');setMessage(`We sent a 6-digit code to +1 ${phone}`);setStep('otp')}
 const verify=e=>{e.preventDefault();if(!/^\d{6}$/.test(otp))return setError('Enter the 6-digit verification code.');onSuccess?.()}
 const google=()=>setMessage('Google sign-in is ready for OAuth connection.')
 const emailContinue=e=>{e.preventDefault();if(!/^\S+@\S+\.\S+$/.test(email))return setError('Please enter a valid email address.');setMessage('Check your email for a verification link.')}
 return <main className="auth-page"><div className="auth-visual"><div className="visual-brand"><img src={LOGO_URL}/><span>Clean America<br/><b>Dallas</b></span></div><div className="visual-copy"><span>OUTDOOR SERVICES, SIMPLIFIED</span><h2>A cleaner space<br/><em>starts here.</em></h2><p>Book trusted local service professionals in a few simple steps.</p></div><div className="visual-chip"><b>✓</b><span><strong>Trusted service</strong><small>Professional & reliable</small></span></div></div><section className="auth-card"><div className="mobile-brand"><img src={LOGO_URL}/><span>Clean America <b>Dallas</b></span></div><div className="auth-heading"><span className="kicker">{mode==='signin'?'WELCOME BACK':'GET STARTED'}</span><h1>{mode==='signin'?'Sign in to your account':'Create your account'}</h1><p>{mode==='signin'?'Manage your bookings, services and schedule.':'Set up your account and book your first service.'}</p></div><div className="mode-switch"><button className={mode==='signin'?'active':''} onClick={()=>reset('signin')}>Sign In</button><button className={mode==='signup'?'active':''} onClick={()=>reset('signup')}>Sign Up</button></div>{step==='otp'?<form className="auth-form" onSubmit={verify}><button type="button" className="back-link" onClick={()=>setStep('form')}>← Change phone number</button><div className="otp-box"><span>VERIFICATION CODE</span><strong>{message}</strong></div><label>6-digit OTP<input className="otp-input" value={otp} onChange={e=>setOtp(e.target.value.replace(/\D/g,'').slice(0,6))} inputMode="numeric" autoComplete="one-time-code" placeholder="000000" maxLength="6" autoFocus/></label><button className="primary" type="submit">Verify & Continue <span>→</span></button><button type="button" className="text-button" onClick={()=>setMessage(`A new code was sent to +1 ${phone}`)}>Resend code</button></form>:<><div className="method-switch"><button className={method==='phone'?'active':''} onClick={()=>{setMethod('phone');setError('')}}>Phone</button><button className={method==='email'?'active':''} onClick={()=>{setMethod('email');setError('')}}>Email</button></div>{method==='phone'?<form className="auth-form" onSubmit={sendOtp}>{mode==='signup'&&<label>Full name<input value={name} onChange={e=>setName(e.target.value)} placeholder="Your full name" autoComplete="name"/></label>}<label>Phone number<div className="phone-field"><span>+1</span><input value={phone} onChange={e=>setPhone(e.target.value.replace(/\D/g,'').slice(0,10))} placeholder="214 555 0188" inputMode="numeric" autoComplete="tel"/></div></label>{mode==='signup'&&<label>Email <small>Optional</small><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" autoComplete="email"/></label>}<button className="primary" type="submit">Send OTP <span>→</span></button></form>:<form className="auth-form" onSubmit={emailContinue}>{mode==='signup'&&<label>Full name<input value={name} onChange={e=>setName(e.target.value)} placeholder="Your full name"/></label>}<label>Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" autoComplete="email"/></label><button className="primary" type="submit">Continue with Email <span>→</span></button></form>}<div className="or"><i/>or<i/></div><button className="google" onClick={google}><span>G</span> Continue with Google</button></>}{error&&<div className="error">{error}</div>}{message&&step==='form'&&<div className="notice">{message}</div>}<p className="legal">By continuing, you agree to our <u>Terms</u> and <u>Privacy Policy</u>.</p></section></main>
}
