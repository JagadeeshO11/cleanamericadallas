import { useState } from 'react'
import './BookingFlow.css'
export default function BookingFlow({service,services,setPage,addBooking}){const [step,setStep]=useState(1);const [date,setDate]=useState('2026-08-20');const [time,setTime]=useState('10:00 AM - 12:00 PM');const [notes,setNotes]=useState('');const [chosen,setChosen]=useState(service||services[0]);const submit=()=>{addBooking({id:`CA-${Date.now().toString().slice(-4)}`,service:chosen.name,date:new Date(date).toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric'}),time,employee:'Awaiting assignment',status:'Scheduled',price:chosen.price,image:chosen.image});setPage('bookings')};return <main className="page-view booking-flow-view"><header className="inner-header"><button onClick={()=>setPage('home')}>‹</button><h1>Book a Service</h1><span/></header><div className="booking-stepper">{['Service','Schedule','Details','Payment'].map((x,i)=><div className={step===i+1?'active':step>i+1?'done':''} key={x}><b>{i+1}</b><small>{x}</small></div>)}</div>{step===1&&<section className="flow-panel"><h2>Choose your service</h2><div className="choice-grid">{services.slice(0,4).map(s=><button className={chosen.id===s.id?'selected':''} key={s.id} onClick={()=>setChosen(s)}><span>{s.icon}</span><div><strong>{s.name}</strong><small>{s.description}</small></div><b>${s.price}</b></button>)}</div></section>}{step===2&&<section className="flow-panel"><h2>When should we come?</h2><label>Date<input type="date" value={date} onChange={e=>setDate(e.target.value)}/></label><label>Time<select value={time} onChange={e=>setTime(e.target.value)}><option>10:00 AM - 12:00 PM</option><option>12:00 PM - 2:00 PM</option><option>2:00 PM - 4:00 PM</option><option>4:00 PM - 6:00 PM</option></select></label></section>}{step===3&&<section className="flow-panel"><h2>Booking details</h2><div className="booking-summary"><img src={chosen.image} alt=""/><div><strong>{chosen.name}</strong><b>${chosen.price}</b><small>1234 Elm Street, Dallas, TX</small></div></div><label>Special instructions<textarea value={notes} onChange={e=>setNotes(e.target.value)} placeholder="Anything the employee should know?"/></label></section>}{step===4&&<section className="flow-panel"><h2>Payment summary</h2><div className="checkout-summary"><p><span>Service fee</span><b>${chosen.price}.00</b></p><p><span>Platform fee</span><b>$5.00</b></p><hr/><p className="total"><span>Total</span><b>${chosen.price+5}.00</b></p></div><small>Payment is collected securely after confirmation.</small></section>}<div className="flow-footer">{step>1&&<button className="secondary-btn" onClick={()=>setStep(step-1)}>Back</button>}<button className="primary-btn" onClick={()=>step<4?setStep(step+1):submit()}>{step<4?'Continue':'Confirm Booking'}</button></div></main>}
